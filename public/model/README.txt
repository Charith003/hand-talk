Drop your exported TF.js model files here:
  - model.json
  - group1-shard1of1.bin (and any additional shards)

Also place labels.json at /public/labels.json (NOT inside /public/model/).
The order of labels must match the training class order exactly.